spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/locals?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: business
    password: business
  rabbitmq:
    username: root
    password: kotNaPlecach
  jackson:
    mapper:
      ACCEPT_CASE_INSENSITIVE_ENUMS: true